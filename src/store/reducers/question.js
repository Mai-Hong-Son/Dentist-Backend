
const initialState = {
};

export default (state = initialState, action) => {

  switch (action.type) {
    // case 'question/status':
    //   return {
    //     ...state,
    //     statuses: action.payload,
    //   };
    default:
  }

  return state;
};

